const component = require("./component.class")
const configuration = require("./configuration.class")
const controller = require("./controller.class")
const middleware = require("./middleware.class")
const router = require("./router.class")
const service = require("./service.class")
const library = require("./library.class")
const model = require("./model.class")
const custom = require("./custom.class")

module.exports = {
    component,
    configuration,
    controller,
    middleware,
    router,
    service,
    library,
    model,
    custom,
}